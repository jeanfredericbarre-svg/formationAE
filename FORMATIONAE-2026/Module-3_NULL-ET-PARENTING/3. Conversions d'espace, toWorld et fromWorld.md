# Conversions d'espace, toWorld et fromWorld

Dans After Effects, les coordonnées d'un calque ne sont pas absolues. Elles dépendent du **référentiel** (son propre monde) dans lequel elles sont exprimées. AE en utilise trois.

**Le Local Space** est le système de coordonnées propre à chaque calque. Le point `[0, 0]` correspond au coin supérieur gauche du calque, indépendamment de sa position dans la composition. Si tu déplaces, fais pivoter ou scales le calque, ses coordonnées internes ne changent pas.

**Le Comp Space** est le système de coordonnées de l'écran, en pixels. Le point `[0, 0]` est le coin supérieur gauche de la composition. C'est ce que tu vois dans le panneau de prévisualisation. En 2D, c'est le référentiel de travail principal.

**Le World Space** est le système de coordonnées 3D global de la scène, indépendant de la caméra. En 2D, il est identique au Comp Space. La distinction n'existe vraiment qu'en 3D, quand des calques ont des rotations dans l'espace ou quand une caméra est présente.

Dès qu'un calque est **parenté** à un autre, sa propriété `Position` affiche des coordonnées en local space par rapport à son parent, pas par rapport à la composition. Pour faire communiquer deux calques qui ont des parents différents, on ne peut pas simplement comparer leurs valeurs de `Position` brutes : elles ne parlent pas le même langage. C'est précisément pour ça qu'existent `toComp()`, `fromComp()`, `toWorld()` et `fromWorld()`.

<div class="ae-download">
  <div class="ae-download__icon">⬇</div>
  <div class="ae-download__body">
    <span class="ae-download__label">Projet After Effects</span>
    <span class="ae-download__name">Module-3_3_01-Reference Spatiale.aep</span>
    <span class="ae-download__desc">Tester et mettre en pratique time et valueAtTime()</span>
  </div>
  <a class="ae-download__btn" href="https://drive.google.com/uc?export=download&id=1TM2QOmKub9ELy0tQM34mifgY9ZsZBw-8">Télécharger</a>
</div>


## Sommaire
- [La famille complète des méthodes de conversion](#methodes)
- [toComp() et fromComp()](#tocompfromcomp)
- [toWorld() et fromWorld()](#toworldfromworld)
- [Cas pratiques](#caspratiques)

---

<span id="methodes"></span>
## La famille complète des méthodes de conversion

AE fournit quatre méthodes pour convertir des coordonnées entre référentiels. Elles existent par paires, chaque méthode a son inverse.

| Méthode | Direction | Référentiel source | Référentiel cible |
|---|---|---|---|
| `toComp(point)` | → | Local du calque | Composition |
| `fromComp(point)` | ← | Composition | Local du calque |
| `toWorld(point)` | → | Local du calque | World (3D) |
| `fromWorld(point)` | ← | World (3D) | Local du calque |

En **2D**, `toComp` et `toWorld` retournent des résultats identiques, la composition et le world sont le même référentiel. La distinction n'existe vraiment qu'en **3D**, quand des calques ont des orientations différentes dans l'espace.

---

<span id="tocompfromcomp"></span>
## toComp() et fromComp()

`toComp()` et `fromComp()` opèrent dans l'espace **2D de la composition**, c'est-à-dire les coordonnées en pixels telles qu'elles apparaissent à l'écran. C'est la paire la plus utilisée en 2D pour faire communiquer des calques qui ont des parents différents.

```javascript
thisLayer.toComp([0, 0])
// → position du pivot du calque en coordonnées de composition
//   retourne un Array [x, y]
```

**Exemple : faire pointer un calque vers un autre :**

```javascript
// Sur la propriété Rotation du calque "Flèche" :
const cible = thisComp.layer("Cible").toComp([0, 0]);
const source = thisLayer.toComp([0, 0]);

const dx = cible[0] - source[0];
const dy = cible[1] - source[1];

radiansToDegrees(Math.atan2(dy, dx))
// → calcule l'angle entre les deux calques et l'applique à la rotation
```

---
<span id="toworldfromworld"></span>
## toWorld() et fromWorld()

`toWorld()` et `fromWorld()` opèrent dans l'espace **3D global** de la scène, indépendamment de toute caméra ou point de vue.

```javascript
thisLayer.toWorld([0, 0, 0])
// → position du pivot du calque dans l'espace 3D mondial
//   retourne un Array [x, y, z]
```

Ces méthodes sont particulièrement utiles quand on travaille avec des calques 3D qui ont des rotations dans les 3 axes, le world space garantit des coordonnées absolues, non affectées par les rotations des parents.

```javascript
// Trouver la position 3D réelle d'un point quelconque d'un calque :
thisLayer.toWorld([100, 50, 0])
// → les coordonnées mondiales du point situé à [100, 50] en local sur ce calque
```

---

<span id="caspratiques"></span>
## Cas pratiques

### Attacher un calque au coin d'un autre calque

```javascript
// Sur la propriété Position du calque "Badge" :
const hote = thisComp.layer("Carte");

// Coin supérieur droit de "Carte" en local space :
const coinLocal = [hote.width / 2, -hote.height / 2];

// Convertir en coordonnées de composition :
const coinComp = hote.toComp(coinLocal);

// Convertir en local space du calque "Badge" :
thisLayer.fromComp(coinComp)
```

Le Badge se colle au coin de la Carte, et si la Carte bouge, tourne ou change de parent, le Badge suit automatiquement.

### Connecter deux calques avec une ligne

Pour calculer la longueur et l'angle entre deux calques (utile pour un calque Shape ou un masque) :

```javascript
// Position des deux calques en espace composition :
const p1 = thisComp.layer("Point A").toComp([0, 0]);
const p2 = thisComp.layer("Point B").toComp([0, 0]);

const dx = p2[0] - p1[0];
const dy = p2[1] - p1[1];

// Longueur de la ligne :
const longueur = Math.sqrt(dx * dx + dy * dy);

// Angle :
const angle = radiansToDegrees(Math.atan2(dy, dx));
```

> 💡 `Math.atan2(dy, dx)` est une fonction JavaScript standard, elle retourne l'angle en **radians** entre deux points. `radiansToDegrees()` est une méthode propre à AE qui convertit ce résultat en degrés, utilisable directement comme valeur de Rotation.
